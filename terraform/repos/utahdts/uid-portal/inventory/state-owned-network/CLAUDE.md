# Organization-owned network inventory only

The Terraform-shaped files below this directory are historical inventory of
the State of Utah organization-owned VPC. Never run `terraform plan`,
`terraform apply`, imports, or network/tag mutations here. Application work may
read the recorded identifiers, but every network repair or lifecycle change
belongs to the State network owner.
